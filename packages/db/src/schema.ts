/**
 * Drizzle schema — intentionally empty.
 *
 * The schema shape is a confirmation checkpoint: it is expensive to change once
 * data exists, so it gets reviewed before the first migration is generated.
 */
export {};
